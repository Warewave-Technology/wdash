flask==2.3.3
flask-login==0.6.3
authlib==1.2.1
elasticsearch==8.10.0
python-dotenv==1.0.0
pyyaml==6.0.1
gunicorn==21.2.0
# Metadata store: WDash's own state (accounts, roles, sources, dashboards).
# Deliberately not Elasticsearch — that is a data source and may be absent.
sqlalchemy==2.0.51
# LGPL v3, and only needed when DATABASE_URL points at Postgres — SQLite needs
# nothing. Kept here because a metadata store that cannot be pointed at a real
# database is not one; move it to an extra if a build has to ship without any
# copyleft at all.
psycopg[binary]==3.3.4
# Secrets at rest and password hashing for the local break-glass account.
cryptography==50.0.0
argon2-cffi==23.1.0
# The QR code on the authenticator enrolment page, and nothing else. BSD
# 3-clause, pure Python, and no dependencies of its own on the versions we
# run — so it adds one file of drawing code to the image and no transitive
# tree. The TOTP itself is ours, in src/wdash/auth/totp.py: RFC 6238 is two
# pages of hmac and hashlib, checked against the RFC's own vectors, and a
# third party in the authentication path would be a worse trade than a
# third party in the picture beside it.
segno==1.6.6
# Directory authentication. LGPL v3, like psycopg above — kept behind
# src/wdash/auth/ldap_auth.py so it can be swapped in one file. Both are used
# as libraries over their published interfaces, which is what the LGPL is for;
# neither pulls WDash's own licence with it.
ldap3==2.9.1
requests==2.32.5